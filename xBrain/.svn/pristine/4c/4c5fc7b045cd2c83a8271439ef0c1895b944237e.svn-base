#include <iostream>
#include "DataNode.H"
#include "CJSONTest.h"
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char** argv) 
{
	
	int *p = NULL, *p2 = NULL;
	if (!p) printf("\nNULL......\n");
	DS::DataNode *dn = new DS::DataNode(100);
	dn->PushData("HELLOWORLD", sizeof("HELLOWORLD"));
	
	
	printf("%s", dn->GetBuff());
	
	DoTest();
	return 0;
}
